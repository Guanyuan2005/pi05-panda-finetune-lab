# π0.5 项目本地实现与验证说明

## 2026-09-10 正式数据就绪状态（覆盖下方早期计划）

当前任务已升级为 `panda_multi_object_box_v2`：Panda 根据英文指令抓取苹果、橙子、蓝色罐或黄色盒子，并放入绿色箱子。

- train：400 episodes（300 正常；四类恢复各 25），94,064 frames，完整校验通过；
- validation：60 episodes（40 正常；四类恢复各 5），14,875 frames，完整校验通过；
- test：100 个冻结场景（50 正常、50 恢复），seed 50000–50099，不含教师动作；
- train / validation / test seed 无交叉；
- 正式动作回放 5/5 成功，60-episode 抽样审计通过；
- 正式数据根目录：`data/lerobot/panda_multi_object_box_v2`；
- 最终上传目录：`upload/pi05_panda_multi_object_box_v2`。

下方旧的 apple/tray v1 内容保留为实现演进记录；执行命令和数据版本以本节、`configs/dataset_v1.yaml` 及最终 readiness 报告为准。


> 最短流程：安装本地环境 → 看五类样例视频 → 运行教师成功率测试 → 小批量采集 → 校验与纯动作回放 → 分片并行正式采集 → 合并 → 生成带校验和的上传包。

## 1. 当前结论

本地代码链路已经完整实现并跑通：自定义 MuJoCo 任务、双相机、平滑路径教师、四类扰动与重规划、LeRobot 写入、分片并行、合并、数据校验、纯动作回放、样例录像和上传包都已有可运行脚本。

项目内还生成了一个 5 回合、977 帧的双相机冒烟数据集：`data/lerobot/panda_multi_object_box_smoke`。它用于证明格式和回放链路，不代替计划中的 400 个训练回合与 60 个验证回合。正式批量采集应在人工看完样例视频之后进行。

## 2. 实际实现了什么

### 任务与相机

- 任务固定为“Panda 把红色苹果放入绿色托盘”；
- MuJoCo 负责机器人动力学、桌面、苹果、托盘、干扰物和接触；
- 外部相机观察整个工作区，腕部相机随 hand 运动；
- 每个 seed 决定苹果、1 个干扰物和托盘位置，可以完全复现；
- 模型输入只包含两路 RGB、8 维状态和英文指令，不包含物体真值位置。

配置位于 `configs/panda_apple_task.yaml`、`configs/camera_v1.yaml` 和 `configs/dataset_v1.yaml`。

本轮实测环境为 Python 3.12、MuJoCo 3.12.0、LeRobot 0.4.4、PyTorch 2.14.0 CPU、TorchVision 0.29.0、PyAV 18.1.0。

### 教师与路径

`pi05_local/teacher.py` 实现了执行与恢复状态机。抓取后不再固定“升到 0.54 m、水平移动、再下降”，而是：

1. 根据路径附近干扰物高度计算最低安全净空；
2. 生成直达、左绕和右绕三条三次 Bézier 候选路径；
3. 沿路径逐点 IK，并以前一点关节角作为下一点初值；
4. 拒绝有机器人碰撞的候选；
5. 按 TCP 路程、关节路程、jerk 和净空选成本最低的路径；
6. 用带单步关节硬上限的平滑插值执行。

这里的“优化”是有安全约束的候选路径优化，不宣称求得数学上的全局最优解。

### 纠错能力

四种可观察扰动都保存从出错前到恢复成功的完整 episode：

| 类型 | 发生的错误 | 教师怎样恢复 |
| --- | --- | --- |
| `approach_offset` | 第一次接近位置偏移 | 撤离、重新定位、改变抓取中心再抓 |
| `weak_grasp` | 第一次夹爪闭合不足 | 检测未抬起，打开并重抓 |
| `object_slip` | 抬升后苹果滑落到安全位置 | 停止旧轨迹，重新定位并重规划 |
| `place_offset` | 第一次放到托盘外侧 | 检测放置失败，重新抓取并放置 |

重试不是简单重复旧动作：系统重新读取苹果位置，并在后续尝试中小幅搜索抓取中心、调整抓取高度和夹持量。最多重试 2 次；仍失败的回合只写失败元数据，不进入监督训练集。

### 教师为何默认使用仿真真值

`--teacher-source privileged` 表示教师在 MuJoCo 中读取苹果位置来生成高质量标签。这是仿真数据生成器的“特权教师”，不是模型输入作弊：写入 LeRobot 的 observation 中没有真值位置，π0.5 仍只能从图像和关节状态学习。

同时保留了 `--teacher-source vision`，它调用现有多相机 RGB-D 定位；已经通过 1 个完整回合冒烟测试。批量采集默认用特权教师，原因是先把研究重点放在视觉策略学习与恢复数据，而不是让传统视觉定位误差污染监督动作。以后可单独比较两种教师。

### 数据格式与时序

每 0.1 秒严格按“先读 observation，再记录 action，再执行 action，再推进仿真”的顺序写入：

| LeRobot 字段 | shape | 含义 |
| --- | --- | --- |
| `observation.images.image` | 3×256×256 | 外部 RGB |
| `observation.images.wrist_image` | 3×256×256 | 腕部 RGB |
| `observation.state` | 8 | q1～q7 + 归一化夹爪位置 |
| `action` | 8 | 7 个绝对关节目标 + 夹爪目标 |
| `task` | string | 固定英文指令 |

默认使用 `--image-storage image`。这是当前环境实际完成写入、读取、合并和回放验证的模式。压缩视频可以编码，但当前 TorchVision 与 LeRobot 0.4.4 的视频解码接口不兼容；代码已增加前置检查，除非安装兼容的 TorchCodec，否则不会允许产生事后无法读取的视频数据。用于人工观看的 MP4 不受此限制。

### 并行采集

可以并行，但每个进程只能写自己的 `worker-XX` 分片。主进程按 seed 交错分配任务，worker 独立 finalize；完成后逐 episode 读取并重写到最终数据集，不能直接拼 Parquet 文件。

最终用 4 worker 同时采集四类纠错，各分片均通过校验；合并后为 4 episodes / 929 frames，并再次通过整体校验。此前的 2-worker 数据也完成了纯动作回放。纠错类型按全局成功回合编号轮换，不会因每个 worker 从零计数而全变成同一种错误。

## 3. 文件说明

| 文件 | 作用 |
| --- | --- |
| `pi05_local/environment.py` | 场景、相机、任务 seed、IK 路径候选、碰撞检查、成功判定 |
| `pi05_local/teacher.py` | 10 Hz 教师状态机、扰动、失败检测和重新规划 |
| `pi05_local/dataset.py` | LeRobot schema、分片采集、元数据、校验和工具 |
| `scripts/collect_panda_apple.py` | 单进程或多进程采集 |
| `scripts/evaluate_teacher.py` | 不写图像的 50-seed/纠错成功率预检 |
| `scripts/validate_panda_dataset.py` | schema、数值、动作连续性、恢复事件和跨 split 检查 |
| `scripts/merge_panda_shards.py` | 按 episode 安全合并并重新编号 |
| `scripts/replay_panda_episode.py` | 不运行教师，只执行数据集 action，并录像判定结果 |
| `scripts/render_sample_videos.py` | 生成正常与四类纠错双相机视频 |
| `scripts/generate_test_scenes.py` | 冻结 50 个正常与 50 个纠错测试场景，不保存教师动作 |
| `scripts/build_upload_bundle.py` | 生成上传目录、manifest 和 SHA-256 |
| `scripts/setup_local_env.sh` | 安装 MuJoCo/LeRobot 本地运行环境 |
| `tests/test_local_pipeline.py` | 确定性、schema、恢复轮换、轨迹步长、安全删除和集成测试 |

## 4. 已执行的验证

| 检查 | 结果 |
| --- | --- |
| Python 语法编译 | 通过 |
| 自动测试 | 6/6 通过，包括一个完整 MuJoCo 教师回合 |
| 普通教师固定 50 seeds | 49/50，98%，超过 90% 门槛 |
| 四类纠错固定 20 seeds | 18/20，90%，达到门槛 |
| RGB-D 视觉教师 | 1/1 完整成功 |
| 项目冒烟数据集 | 5 episodes、977 frames，通过，无错误/警告 |
| 动作范围 | 关节目标不越界；数据集最大任意维相邻跳变 0.1565，低于 0.20 门槛 |
| 图像 | 两路均为 3×256×256，非全黑/全白，随帧变化 |
| 分片并行与合并 | 4 workers → 四类纠错各 1 → 4 episodes/929 frames，通过 |
| 合并后纯动作回放 | 原记录成功，重新回放也成功 |
| 上传包 | 40 个文件，独立复核 SHA-256 无不一致 |
| 样例 MP4 | 5 类视频均可解码，13.6～27.5 秒 |

98% 和 90% 不是“教师永不失败”。采集器会继续尝试新 seed，只有最终成功回合进入数据集；失败原因保留在 `meta/pi05_failures.jsonl`。正式采集前仍建议人工观看下面的视频。

## 5. 直接观看样例

- `reports/sample_videos/overview.png`：五类样例的双相机总览；
- `reports/sample_videos/normal.mp4`：正常抓放；
- `reports/sample_videos/approach_offset.mp4`：接近偏移后重抓；
- `reports/sample_videos/weak_grasp.mp4`：未夹稳后重抓；
- `reports/sample_videos/object_slip.mp4`：滑落后重新定位；
- `reports/sample_videos/place_offset.mp4`：放偏后重新抓放；
- `reports/sample_videos/dataset_episode_0000.mp4`：从 LeRobot 数据读取 action 的独立回放。

当前样例要求六个视频最终全部成功。 `dataset_episode_0000.mp4` 现在来自 `data/lerobot/panda_multi_object_box_smoke_v2`；旧 smoke 数据使用修改前的苹果惯量，只保留作历史对照。场景只保留 1 个随机干扰物；苹果使用平滑高模显示、居中球形质量/碰撞代理，并加入滚动阻力和自由关节阻尼，避免平地持续晃动。正式训练集按 75% 正常成功、25% 纠错成功组成，四类纠错均分；任何最终失败回合只保存到诊断集。

重新生成：

~~~bash
python scripts/render_sample_videos.py
~~~

## 6. 本地运行命令

在 `pi05-finetune-lab` 中执行：

~~~bash
./scripts/setup_local_env.sh
source .venv/bin/activate
export PYTHONPATH="$PWD:$PWD/../mujoco_arm_lab"

# 自动测试与教师门槛
python -m unittest discover -s tests -v
python scripts/evaluate_teacher.py --episodes 50 --mode nominal
python scripts/evaluate_teacher.py --episodes 20 --mode recovery --seed-start 200

# 先做小数据
python scripts/collect_panda_apple.py --split train --mode mixed --episodes 5 \
  --seed-start 1000 --dataset-root data/lerobot/panda_multi_object_box_smoke \
  --image-storage image --overwrite --headless
python scripts/validate_panda_dataset.py --dataset-root data/lerobot/panda_multi_object_box_smoke
python scripts/replay_panda_episode.py \
  --dataset-root data/lerobot/panda_multi_object_box_smoke --episode 0
~~~

人工看完样例后再正式并行采集：

~~~bash
python scripts/collect_panda_apple.py --split train --mode nominal --episodes 300 \
  --seed-start 10000 --workers 2 --staging-root data/staging/train/nominal \
  --image-storage image --overwrite --headless
python scripts/collect_panda_apple.py --split train --mode recovery --episodes 100 \
  --seed-start 11000 --workers 2 --staging-root data/staging/train/recovery \
  --image-storage image --overwrite --headless
python scripts/collect_panda_apple.py --split validation --mode nominal --episodes 40 \
  --seed-start 20000 --workers 2 --staging-root data/staging/validation/nominal \
  --image-storage image --overwrite --headless
python scripts/collect_panda_apple.py --split validation --mode recovery --episodes 20 \
  --seed-start 21000 --workers 2 --staging-root data/staging/validation/recovery \
  --image-storage image --overwrite --headless
python scripts/validate_panda_dataset.py --shards-root data/staging
python scripts/merge_panda_shards.py \
  --shards-root data/staging --output-root data/lerobot/panda_multi_object_box_v2 --overwrite
python scripts/validate_panda_dataset.py --dataset-root data/lerobot/panda_multi_object_box_v2
python scripts/build_upload_bundle.py \
  --dataset-root data/lerobot/panda_multi_object_box_v2 --output upload/pi05_panda_multi_object_box_v2 --overwrite
~~~

建议先保留 `workers=2`。是否改成 4 应根据内存、CPU、EGL 稳定性和每小时成功回合数实测决定；更多进程不保证线性加速。

## 7. 仍未在本轮执行的耗时工作

- 400 个训练回合和 60 个验证回合尚未批量生成；
- 尚未申请 GPU，也没有做 OpenPI 适配、norm stats、JAX LoRA 或远程 policy server；
- 50/20 教师验证达到了第一版门槛，但仍有少数随机场景失败，后续可继续优化抓取姿态与放置恢复；
- 正式数据生成后，还需随机人工观看至少 20 个正常回合和每类 10 个恢复回合。

这些工作没有被冒烟测试替代；它们是确认本地轨迹质量后，申请云端算力之前的下一步。
