# 算力平台申请前的本地工作清单

## 2026-09-10 正式数据就绪状态（覆盖下方早期计划）

当前任务已升级为 `panda_multi_object_box_v2`：Panda 根据英文指令抓取苹果、橙子、蓝色罐或黄色盒子，并放入绿色箱子。

- train：400 episodes（300 正常；四类恢复各 25），94,064 frames，完整校验通过；
- validation：60 episodes（40 正常；四类恢复各 5），14,875 frames，完整校验通过；
- test：100 个冻结场景（50 正常、50 恢复），seed 50000–50099，不含教师动作；
- train / validation / test seed 无交叉；
- 正式动作回放 5/5 成功，60-episode 抽样审计通过；
- 正式数据根目录：`data/lerobot/panda_multi_object_box_v2`；
- 最终上传目录：`upload/pi05_panda_multi_object_box_v2`。

下方旧的 apple/tray v1 内容保留为实现演进记录；执行命令和数据版本以本节、`configs/dataset_v1.yaml` 及最终 readiness 报告为准。


> 目标：在不租用 GPU 的情况下，把任务、教师、数据采集、恢复示范、LeRobot 数据集和待上传文件全部准备好。  
> 原则：本清单未通过前，不申请算力平台。

## 0. 本地阶段最终要得到什么

本地阶段不是训练 π0.5。最终应得到：

~~~text
1. 一个稳定的自定义 MuJoCo 任务
2. 一个能生成短、快、平滑且安全路径，并在失败后重规划的教师
3. 一个同步记录双路 RGB、state、action 和 task，且支持分片并行的采集器
4. 一批自己的正常成功示范
5. 一批自己的扰动恢复成功示范
6. 一个通过回放和统计检查的 LeRobot 数据集
7. 一份可直接上传算力平台的目录
~~~

本地最终目录建议：

~~~text
pi05-finetune-lab/
├── configs/
│   ├── panda_apple_task.yaml
│   ├── camera_v1.yaml
│   └── dataset_v1.yaml
├── data/
│   ├── staging/
│   │   ├── train/worker-*/
│   │   └── validation/worker-*/
│   ├── lerobot/
│   │   └── panda_multi_object_box_v2/
│   ├── failed_episodes/
│   ├── test_scenes.json
│   └── manifest.json
├── scripts/
│   ├── collect_panda_apple.py
│   ├── evaluate_teacher.py
│   ├── render_sample_videos.py
│   ├── generate_test_scenes.py
│   ├── merge_panda_shards.py
│   ├── validate_panda_dataset.py
│   ├── replay_panda_episode.py
│   ├── build_upload_bundle.py
│   └── setup_local_env.sh
├── reports/
│   ├── teacher_validation.md
│   ├── parallel_benchmark.md
│   ├── dataset_validation.md
│   └── sample_videos/
├── tests/test_local_pipeline.py
├── 本地实现与验证说明.md
└── upload/
    └── pi05_panda_apple_v1/
~~~

上述脚本已在本轮实现并完成冒烟验证；实际结果和限制见《本地实现与验证说明》。

## 1. 冻结第一版任务

### 1.1 固定定义

| 项目 | 第一版决定 |
| --- | --- |
| 机器人 | Franka Panda |
| 仿真 | 自定义 mujoco_arm_lab |
| 目标物 | 红色苹果 |
| 放置目标 | 新增绿色托盘 |
| 干扰物 | 从杯子、香蕉、药盒、洗护瓶和马克笔中固定随机选择 3 个 |
| 指令 | Pick up the red apple and place it in the green tray. |
| 控制频率 | 10 Hz |
| 最大重试 | 2 次 |
| 单回合超时 | 先按教师最长成功时间的 1.5 倍确定 |

第一版只做这一条指令，不同时训练多物体、多语言和多任务。这样能先判断失败来自数据与接口，还是来自任务复杂度。

### 1.2 成功条件

必须由程序自动判断：

- 苹果中心位于托盘有效 XY 区域内；
- 苹果高度与托盘底面相符，没有悬空或掉到桌下；
- 夹爪已经释放苹果；
- 苹果连续稳定至少 1 秒；
- 机械臂没有越界或严重碰撞。

同时记录阶段性结果：是否看见苹果、是否到达预抓取点、是否双指接触、是否抬升、是否搬运到托盘上方和是否稳定放置。

## 2. 不推翻现有工程，但要补齐五个能力

现有 panda_vision_grasp.py 已有随机场景、真实苹果、RGB-D 定位、IK、抓放轨迹和成功判定。它适合做教师起点，但不能直接作为 π0.5 数据集。

需要新增：

1. **绿色托盘**：有明确视觉外观、碰撞几何和有效放置区域；
2. **腕部相机**：挂在 Panda hand 下，接近苹果时能看清苹果和两指；
3. **优化搬运路径**：用自适应净空的平滑三维弧线取代固定的“上—平移—下”路径；
4. **10 Hz 数据记录器**：同步保存模型真正会使用的观测和动作；
5. **可重规划状态机**：检测抓取或放置失败后重新观察，而不是继续执行旧轨迹。

其他三台外部相机可保留，但第一版只选一台作为模型主视图，其余只做诊断。

## 3. 教师控制器怎样优化

### 3.1 规划教师与手工操作

主要数据使用规划教师。手工操作只在以下情况使用：

- 检查相机视角是否合理；
- 确认托盘和物体碰撞是否自然；
- 标注自动失败原因；
- 少量覆盖规划器始终无法进入的特殊恢复状态。

不要把低质量手工轨迹与规划轨迹不加区分地混在一起。若保留手工示范，元数据中写 teacher_type=manual，并单独统计。

### 3.2 从一次性轨迹改成状态机

建议状态：

~~~text
OBSERVE
PREGRASP
DESCEND
CLOSE
VERIFY_GRASP
LIFT
VERIFY_LIFT
MOVE_ABOVE_TRAY
PLACE
OPEN
VERIFY_PLACE
RETREAT
SUCCESS
RECOVER
FAIL
~~~

VERIFY_GRASP 检查双指接触和苹果是否随夹爪抬升；VERIFY_LIFT 检查苹果是否滑落；VERIFY_PLACE 检查苹果是否进入托盘并稳定。

检测失败时进入 RECOVER：

1. 停止执行剩余旧轨迹；
2. 打开夹爪并移动到安全高度；
3. 重新渲染和定位苹果；
4. 从当前关节状态重新做 IK；
5. 重试，最多 2 次。

### 3.3 教师验收

批量采集前，用 50 个不同随机 seed 运行教师：

- 总成功率至少 90%；
- 无关节越界；
- 无苹果或干扰物初始穿透；
- 无明显夹爪穿模；
- 成功回合中动作没有不连续大跳；
- 失败原因能够自动分类。

达不到门槛时先修教师，不要用扩大数据量掩盖教师错误。

### 3.4 优化现有“上—平移—下”路径

现有实现的 `lift=[object_x, object_y, 0.54]` 和 `above=[destination_x, destination_y, 0.54]` 会强制先竖直抬升、再水平搬运。修改顺序：

1. 根据苹果、托盘和路径附近干扰物的高度计算最低安全净空；
2. 生成直达弧线、左绕行弧线和右绕行弧线等少量候选 TCP 路径；
3. 对每条路径密集采样，逐点 IK，并做关节限位、自碰和环境碰撞检查；
4. 对可行路径计算 `J = w_time*T + w_tcp*L_tcp + w_joint*L_q + w_jerk*Jerk + w_clear/Clearance`；
5. 选取代价最小的路径，再按关节速度/加速度上限重新分配时间；
6. 规划执行中一旦抓取状态或物体位置已变，立即废弃剩余旧路径并重规划。

抬升不是错误；固定抬得过高、三轴严格分段和无视障碍物才是问题。验收时将新旧教师在同一批 seed 上比较，新教师必须保持成功率和最小净空，同时降低中位执行时间、路径长度和 jerk。

## 4. 相机与模型接口

### 4.1 外部主相机

从现有相机中选择一台：

- 能同时看到苹果、托盘和主要工作区；
- 机械臂接近时不会长期完全遮挡苹果；
- 视角固定并写入 camera_v1.yaml。

### 4.2 腕部相机

新增到 hand body 下。检查：

- 预抓取和下降阶段能看到苹果；
- 闭合时能看到至少部分手指与目标；
- 搬运阶段画面不会只剩机器人自身；
- 训练与部署使用相同位姿和视场角。

### 4.3 固定数据接口

每 0.1 秒记录：

| 字段 | shape | 说明 |
| --- | --- | --- |
| image | 256×256×3 uint8 | 外部 RGB |
| wrist_image | 256×256×3 uint8 | 腕部 RGB |
| state | 8 float32 | q1～q7 + 归一化夹爪位置 |
| action | 8 float32 | 下一步 q_ref1～q_ref7 + 归一化夹爪目标 |
| task | string | 固定英文指令 |

`action` 第一版保存仿真 actuator 真正接收的绝对关节目标，不保存“规划器下一个路径点的索引”，也不在本地数据集中预先换成 delta。这样回放时动作语义是唯一的。

采样顺序必须是：

~~~text
读取 observation_t
  → 计算或取得 action_t
  → 同时写入 observation_t 与 action_t
  → 将 action_t 发给 actuator
  → 推进仿真
~~~

不能把整条规划路径当作逐帧 action，也不能在动作执行后才保存对应 observation。

## 5. 正常示范采集

### 5.1 先做 5 个文件写入测试

确认：

- episode 能正常结束并调用 save_episode；
- 两路图像可打开；
- state/actions shape 固定；
- task 没有缺失；
- success 为真；
- scene seed 被记录。

### 5.2 再做 30 个小数据集

用途：

- 随机回放至少 20 个 episode；
- 检查 RGB/BGR、时间对齐和夹爪方向；
- 在未来云端先做小规模过拟合；
- 估算每个 episode 大小和正式数据磁盘需求。

### 5.3 正式正常数据

教师通过 50-seed 验收后，采集 300 个正常成功 episode。失败回合放入 failed_episodes，不自动混入训练集。

## 6. 恢复示范采集

### 6.1 扰动必须可恢复

扰动大小先从小到大标定。目标是让教师大多数时候能够发现问题并最终完成任务，而不是制造随机失败。

| recovery_type | 注入阶段 | 初始建议 |
| --- | --- | --- |
| approach_offset | PREGRASP | 第一版实测使用 X 方向 6 cm 偏移 |
| weak_grasp | CLOSE | 第一次夹爪闭合量不足 |
| object_slip | LIFT/MOVE | 苹果小幅位移或短脉冲扰动 |
| place_offset | PLACE | 首次放置点偏离托盘中心 |

具体数值必须通过本地可视化验证，不能直接把建议值当最终参数。

### 6.2 什么数据进入训练集

进入训练集的恢复 episode 必须：

- 确实出现了扰动；
- 扰动后观测中能看见偏差；
- 教师停止旧轨迹并重新规划；
- 最终成功；
- 恢复动作平滑且没有危险穿模；
- recovery_type、events[].frame 和 retry_count 完整。

仅记录“扰动前的正常部分 + 最终失败”不能教会模型正确恢复。这类回合只进入失败诊断集。

### 6.3 数量

第一版采集 100 个恢复成功 episode，四类扰动尽量均衡。验证集另外保留 20 个恢复成功 episode。

## 7. LeRobot 数据集与划分

建议直接在采集器中调用 LeRobotDataset.add_frame() 和 save_episode()，避免先生成一套临时格式再转换一次。

数据划分：

~~~text
train：
  300 正常成功 + 100 恢复成功

validation：
  40 正常成功 + 20 恢复成功

test：
  50 正常场景 seed + 50 扰动场景 seed
  不保存教师动作，不进入训练数据
~~~

划分必须按 episode/scene_seed，而不是按 frame。将各划分 seed 固定写入 dataset_v1.yaml 和 test_scenes.json。

## 8. 本地数据检查

validate_panda_dataset.py 至少检查：

- 所有必需字段存在；
- 图像 shape、dtype 和颜色正确；
- state/actions 均为 8 维 float32；
- 无 NaN 或 Inf；
- q_ref 不超过关节限位；
- 相邻 action 跳变不超过设定阈值；
- 两路图像、state 和 action 帧数一致；
- 每个 episode 的 frame index 连续；
- train、validation、test seed 无交叉；
- 正常/恢复数量符合 manifest；
- 失败 episode 没有混入第一版训练集。

replay_panda_episode.py 应在不运行教师规划器的情况下，仅按记录 action 回放 episode。回放结果与原始采集明显不同，通常表示动作语义、时序或初始状态保存有问题。

随机人工查看：

- 20 个正常成功 episode；
- 每类至少 10 个恢复 episode；
- 所有失败类型各若干；
- 外部相机和腕部相机视频。

## 9. 哪些可以以后优化，哪些不能拖到云端

### 9.1 申请算力前必须正确

- state/action 定义；
- 观测与动作时序；
- 腕部相机是否真正有用；
- 教师成功与失败判断；
- 教师路径的安全性、长度、耗时和平滑性门槛；
- 重规划恢复逻辑；
- LeRobot 字段与划分；
- 数据回放；
- 上传目录和校验和。

这些问题到了云端不会自动消失，反而会浪费租卡时间。

### 9.2 第一轮训练后可以继续优化

- 更真实的托盘网格和材质；
- 更丰富的背景与光照；
- 相机扰动范围；
- 在已通过第一版路径质量门槛后，继续优化教师速度、抓取姿态和代价函数权重；
- 扰动种类与难度；
- 指令同义改写；
- 更多目标物类别；
- 数据规模和正常/恢复比例。

第一版不要求场景完美或教师覆盖所有错误，但要求接口、时序和现有恢复数据可信。

## 10. 本地已实现文件

### collect_panda_apple.py

负责：

- 根据 seed 生成场景；
- 运行正常或带扰动的教师；
- 10 Hz 同步记录；
- 写 LeRobot episode；
- 失败回合分流；
- 保存采集元数据。

建议参数：

~~~text
--split train|validation
--episodes N
--mode nominal|recovery|mixed
--seed-start N
--workers N
--worker-id N
--headless
--dataset-root PATH    # 单进程写入测试数据集
--staging-root PATH    # 多进程各写 worker 分片
~~~

`--dataset-root` 用于 5/30 episode 单进程测试；`--staging-root` 用于正式并行采集。两者互斥。

### merge_panda_shards.py

只读取已 `finalize()` 且校验通过的 worker 分片，按 split 和 scene seed 排序，重新分配连续 `episode_index`，并生成唯一的最终 LeRobot 数据集。合并不是直接拼接 Parquet 文件或复制元数据。

### validate_panda_dataset.py

输出机器可读 JSON 和人可读 Markdown 报告；任一关键检查失败时返回非零退出码。

### replay_panda_episode.py

读取指定 episode 的初始状态和 action，重新运行 MuJoCo，并保存外部/腕部视频与最终成功判定。

### build_upload_bundle.py

收集数据、配置、报告和版本信息，生成 manifest 与 SHA-256 校验和，不包含 failed_episodes 的大体积诊断视频，除非手动要求。

## 11. 建议本地执行顺序

以下是当前已经实现并验证过的命令接口：

~~~bash
# 1. 教师正常场景验收
python scripts/evaluate_teacher.py --mode nominal --episodes 50 --seed-start 0

# 2. 5 个写入测试
python scripts/collect_panda_apple.py --split train --mode mixed --episodes 5 --seed-start 1000 --dataset-root data/lerobot/panda_multi_object_box_v2 --headless

# 3. 30 个小数据集
python scripts/collect_panda_apple.py --split train --mode mixed --episodes 30 --seed-start 2000 --dataset-root data/lerobot/panda_multi_object_box_smoke --headless

# 4. 校验与回放
python scripts/validate_panda_dataset.py --dataset-root data/lerobot/panda_multi_object_box_smoke
python scripts/replay_panda_episode.py --dataset-root data/lerobot/panda_multi_object_box_smoke --episode 0

# 5. 正式分片并行采集（这里假设本机基准后选定 2 workers）
python scripts/collect_panda_apple.py --split train --mode nominal --episodes 300 --seed-start 10000 --workers 2 --staging-root data/staging/train/nominal --headless
python scripts/collect_panda_apple.py --split train --mode recovery --episodes 100 --seed-start 11000 --workers 2 --staging-root data/staging/train/recovery --headless
python scripts/collect_panda_apple.py --split validation --mode nominal --episodes 40 --seed-start 20000 --workers 2 --staging-root data/staging/validation/nominal --headless
python scripts/collect_panda_apple.py --split validation --mode recovery --episodes 20 --seed-start 21000 --workers 2 --staging-root data/staging/validation/recovery --headless

# 6. 分片校验、合并、重编号和整体校验
python scripts/validate_panda_dataset.py --shards-root data/staging
python scripts/merge_panda_shards.py --shards-root data/staging --output-root data/lerobot/panda_multi_object_box_v2
python scripts/validate_panda_dataset.py --dataset-root data/lerobot/panda_multi_object_box_v2

# 7. 生成上传包
python scripts/build_upload_bundle.py --dataset-root data/lerobot/panda_multi_object_box_v2 --output upload/pi05_panda_multi_object_box_v2
~~~

正式采集器应按目标比例生成正常和恢复回合；不能仅依靠 episodes 总数假设分布正确。

## 12. 本地数据采集可以并行，但必须分片写入

可以并行。一个 episode 是独立 MuJoCo 仿真，适合用多进程同时跑不同 seed。但不让多个进程同时修改同一个 LeRobotDataset 目录，因为 episode 索引、Parquet writer、视频和元数据都需要一致顺序。

推荐结构：

~~~text
worker-00: seeds 10000, 10004, 10008, ... → staging/train/worker-00/
worker-01: seeds 10001, 10005, 10009, ... → staging/train/worker-01/
worker-02: seeds 10002, 10006, 10010, ... → staging/train/worker-02/
worker-03: seeds 10003, 10007, 10011, ... → staging/train/worker-03/

每个 worker 独立 finalize
  → 分片校验
  → 按 scene_seed 合并并重编 episode_index
  → 整体校验与回放
  → 得到 panda_multi_object_box_v2
~~~

并行实现规则：

- 用多进程，不用多线程共享仿真状态；
- 每个 worker 独立创建 `MjModel`、`MjData`、renderer 和输出目录；
- 每个 seed 只属于一个 worker，并在 manifest 记录 `worker_id`、开始/结束时间和教师版本；
- 单个 worker 内仍按 `add_frame → save_episode → finalize` 写完整分片；
- LeRobot 的 `parallel_encoding=True` 只是并行编码同一 episode 的多路相机，不等于并行运行多个 MuJoCo 环境；
- worker 崩溃时只丢弃它当前未完成 episode，不影响其他分片。

在本机上先比较 1、2、4 个 headless worker 各跑 10 个 episode，记录 episode/小时、CPU 利用率、内存、GPU 显存和失败率。选择吞吐有实际提升且不造成渲染失败的 worker 数；不预先假定 RTX 3050 一定能稳定跑 4 路双相机渲染。

`collect_panda_apple.py` 因此还需提供 `--workers`、`--worker-id`、`--staging-root` 和确定性 seed 分配；`build_upload_bundle.py` 之前增加分片合并步骤。

## 13. 申请算力平台门槛

- [ ] 任务定义和固定英文指令已确认；
- [ ] 绿色托盘和成功判定已实现；
- [ ] 外部主相机已固定；
- [ ] 腕部相机已实现并通过画面检查；
- [ ] state/action shape、单位和方向已写入配置；
- [ ] 教师支持验证、撤离、重定位和最多两次重试；
- [ ] 已用自适应净空的平滑三维路径取代固定“上—平移—下”路径；
- [ ] 新旧教师的成功率、耗时、路径长度、jerk 和净空对比报告已生成；
- [ ] 50-seed 教师成功率不低于 90%；
- [ ] 30-episode 小数据集通过校验和回放；
- [ ] 正常和四类恢复 episode 均有合格样本；
- [ ] 1/2/4 worker 吞吐基准已完成，并固定了稳定并行度；
- [ ] 所有 worker 分片均已 finalize、校验、合并和重编号；
- [ ] 正式 train/validation 数据达到目标数量；
- [ ] test seeds 已冻结且未进入数据集；
- [ ] manifest、报告和 SHA-256 校验和已生成；
- [ ] upload/pi05_panda_multi_object_box_v2 可以直接上传。

全部勾选后再申请 GPU。
