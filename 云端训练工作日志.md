# π0.5 Panda 云端训练工作日志

最后更新：2026-09-11

## 1. 用途与安全

本文持续记录 `pi05-finetune-lab` 的数据上传、GPU 验收、OpenPI 安装、数据适配、LoRA 训练、闭环评测及成果下载。

文档记录可复现的目录、命令、版本、结果和故障处理，但不保存 SSH 密码、Access Key、Secret Access Key 或令牌。

后续每一阶段统一按五个问题记录：

1. 这一阶段要解决什么问题；
2. 为什么选择当前方案；
3. 实际执行了什么；
4. 用什么证据判断成功；
5. 失败会影响什么，下一步如何处理。

## 2. 项目与数据

目标是在 MuJoCo 中训练 π0.5 控制 Franka Panda，根据英文指令抓取苹果、橙子、蓝色罐或黄色盒子，并放入绿色箱子。模型输入为双路 256×256 RGB、8 维 state 和英文 task；输出为 8 维绝对关节目标 action，第一版不使用 delta action。

本地上传目录：

```text
/home/yuan/projects/pi05-finetune-lab/upload/pi05_panda_multi_object_box_v2
```

- train：400 episodes，300 条正常示范，四类恢复示范各 25 条，共 94,064 frames；
- validation：60 episodes，40 条正常示范，四类恢复示范各 5 条，共 14,875 frames；
- test：100 个冻结场景，seed 50000–50099，不包含教师动作；
- 三个 split 的 seed 无交叉，正式动作回放 5/5 成功。

## 3. 云端实例

| 项目 | 值 |
| --- | --- |
| 实例 ID | `<instance-id>` |
| 站点 | 山东一区 |
| SSH 主机 | `<gateway-host>` |
| SSH 端口 | `2233` |
| SSH 用户 | `root@<instance-id>` |
| GPU | NVIDIA GeForce RTX 4090，23,028 MiB |
| NVIDIA 驱动 | 580.105.08 |
| 操作系统 | Ubuntu 22.04.3 LTS |
| 系统 Python | 3.10.12 |

SSH 密码不写入本文。需要重新登录时，从平台实例详情页读取当前密码。

## 4. 已完成操作

### 4.1 登录和硬件验收

为什么先验收：GPU 实例按量计费。如果显卡型号、显存、驱动、操作系统或数据盘不符合要求，继续下载模型和安装依赖只会增加费用，并把环境问题拖到训练阶段。先验收可把“资源选错”和“代码错误”分开。

连接命令：

```bash
ssh -p 2233 -l 'root@<instance-id>' <gateway-host>
```

第一次从截图读取密码时多读了一个相邻字符，服务器返回 `Permission denied`。停止重试并由用户直接复制密码后登录成功，凭据没有写入脚本或项目。

登录后检查了主机名、用户、`nvidia-smi`、Ubuntu、Python、内存和磁盘。GPU、驱动、系统和数据盘满足当前安装要求。JAX 的 CUDA 验证要等 OpenPI 依赖安装完成后执行。

### 4.2 上传包完整性

为什么先做哈希校验：上传大文件时即使目录和文件名齐全，也可能存在中断、截断或内容损坏。若不先排除传输错误，后续数据解码失败或训练异常会被误判为 OpenPI 适配问题。SHA-256 同时验证文件存在性和内容一致性。

远端目录：

```text
/root/shared-nvme/pi05_panda_multi_object_box_v2
```

检查结果：

- 目录结构完整，无需重复上传；
- 总容量约 5.9 GB；
- `SHA256SUMS.json` 含 117 条记录；
- 117 项 SHA-256 全部匹配，结果为 `errors: []`。

### 4.3 工作目录和缓存

为什么全部放到数据盘：OpenPI 环境、CUDA wheels、基础模型、JAX 编译缓存和 checkpoint 会占用数十 GB。系统盘较小且与系统运行有关，写满后可能导致 SSH、软件安装和训练同时失败；数据盘盘容量更大，也更适合保存需要下载回本机的成果。

已创建以下数据盘目录：

```text
/root/shared-nvme/workspace
/root/shared-nvme/openpi-cache
/root/shared-nvme/uv-cache
/root/shared-nvme/huggingface
/root/shared-nvme/lerobot/local
/root/shared-nvme/checkpoints
/root/shared-nvme/logs
```

环境变量保存在 `/root/shared-nvme/pi05_env.sh`，权限为 600，并从 `/root/.bashrc` 自动加载。变量包括 `OPENPI_DATA_HOME`、`UV_CACHE_DIR`、`HF_HOME`、`HF_LEROBOT_HOME` 以及 `/root/.local/bin`。模型、依赖缓存和训练产物因此不会占用较小的系统盘。

为什么持久化环境变量：若只在一个 SSH 会话中 `export`，重新登录后变量会消失，依赖和模型可能再次下载到默认目录。专用环境脚本让每个会话使用同一组路径，也便于检查和修改。

### 4.4 基础工具

为什么使用 `uv`：OpenPI 已提供锁定依赖，`uv` 能按项目要求创建独立 Python 3.11 环境并复用数据盘缓存，降低手工组合 Python、JAX 和 PyTorch 版本造成冲突的风险。

为什么安装 Git LFS：机器人与模型仓库可能用 LFS 管理大文件。提前安装可避免仓库里只得到 LFS 指针文本，而实际权重或资产缺失。

执行了 Ubuntu 软件索引更新，并安装或更新了 `git-lfs`、`curl`、`ca-certificates` 和相关 `libcurl4` 软件包。

随后从 Astral 官方安装脚本安装 `uv 0.12.13` 到 `/root/.local/bin`。安装日志位于：

```text
/root/shared-nvme/logs/uv-install.log
```

### 4.5 OpenPI 版本固定

为什么固定 commit：Panda 适配代码和训练配置依赖 OpenPI 的具体接口。若上游代码变化，同一份数据可能无法复现。因此本次固定为：

```text
215abfb217dbac7d5f1273282331b9b1866c0479
```

该值已保存到远端 `OPENPI_COMMIT_USED.txt`。主仓库工作树干净。

完整递归克隆两次因 GitHub TLS 连接中断而失败。改用 HTTP/1.1、浅克隆和 blob 过滤后，主仓库成功检出。Aloha 与 LIBERO 子模块是第三方示例，当前 Panda 依赖清单没有引用；Aloha 下载长时间无增长，因此中止可选子模块，避免示例代码阻塞核心环境。

### 4.6 LeRobot 本机中转

OpenPI 锁定 LeRobot commit `0cf864870cf29f4738d3ade893e6fd13fbd7cdb5`。`uv sync` 两次在 GitHub Git 流末尾出现 `early EOF`。没有更换依赖版本，因为其他版本可能改变数据接口并破坏锁文件的兼容组合。

服务器下载约 26 KB/s，本机测速约 6.15 MB/s，快约 230 倍。因此从本机下载 GitHub 官方同一 commit 的 15.4 MB 源码包，再通过 SSH 上传。两端 SHA-256 均为：

```text
2455b2ed303778f8bcd570caceae0b22c97908eaaf029dff868d9f237b495c00
```

为什么校验后再安装：本机中转增加了一次传输，只有两端哈希一致才能确认源码没有被截断或改变。

### 4.7 依赖与 GPU 验证

`uv sync --no-install-package lerobot` 从数据盘缓存安装 239 个包；随后从已校验的本地源码安装 LeRobot 0.1.0，并重新确认 OpenPI editable 安装。

验证结果：

- OpenPI import：通过；
- LeRobot import：通过；
- JAX 0.5.3：`[CudaDevice(id=0)]`；
- PyTorch 2.7.1+cu126：`cuda_available=True`；
- GPU：NVIDIA GeForce RTX 4090；
- PyTorch 报告显存：22.03 GiB。

为什么同时检查 JAX 和 PyTorch：正式 LoRA 使用 JAX，必须看到 CUDA device；PyTorch 检查用于独立复核驱动、CUDA runtime 和显卡名称，防止只验证了单一框架。

## 5. 当前状态

第 1 项“准备服务器环境”和第 2 项“安装基础工具与 OpenPI”均已完成。

完成标准不是“命令运行过”，而是同时满足：目录与环境变量可在新会话复用、源码版本已固定、OpenPI 与 LeRobot 可导入、JAX 识别 CUDA device、PyTorch 独立识别 RTX 4090。上述条件均已验证通过。

## 6. 后续顺序

OpenPI 安装完成后，依次注册 LeRobot 数据、实现 Panda 适配、计算 norm stats、运行 100-step 冒烟训练、小数据过拟合、正式 LoRA、启动 policy server、闭环评测冻结测试集，最后下载成果并关闭计费实例。

## 7. 本机查看

本文可直接在本机项目目录查看。远端文件不会自动同步到本机，可通过平台 WebSSH 或 SSH 查看。远端日志统一保存在 `/root/shared-nvme/logs`，关键结果也会持续追加到本文。

## 8. 3/4/5 执行记录（2026-09-11）

### 8.1 Panda 适配与注册

远端新增 `openpi/policies/panda_policy.py`：把原始两路 CHW float 图像转为 OpenPI/Pi05 需要的 HWC uint8，完成字段重命名，保留 8 维 state/action，并对缺失右腕相机置零加 mask。动作不做 delta 变换，因为数据是绝对控制量。

注册 `pi05_panda_multi_object_lora_smoke`（100 steps）和 `pi05_panda_multi_object_lora`（20000 steps），LoRA、batch=1、无 wandb；字段映射为 image/wrist_image/state/action/prompt。

### 8.2 LeRobot 兼容目录

原始数据为 v3.0（`tasks.parquet`、`pi05_episodes.jsonl`），锁定版本读取 v2.0 元数据。因此创建非破坏性兼容视图：数据仅 symlink，不复制、不改原始数据；生成 `tasks.jsonl`、`episodes.jsonl`，并按 parquet 内 `episode_index` 分组统计长度。

结果：train 400 episodes/94064 frames；validation 60 episodes/14875 frames。首次脚本误把每个 parquet 文件当 episode，已修复。

### 8.3 norm stats

只读取 parquet 的 `observation.state` 和 `action` 数值列，不解码图像。原因：Panda 适配不改变这两列，直接统计与完整变换等价。state/action 均 8 维，均值、标准差、分位数有限；已保存到 smoke 与正式配置 assets 目录。

### 8.4 校验与剩余事项

兼容样本已验证：单文件 1634 行，state/action shape 均为 (1634, 8)；元数据和 norm stats 存在。OpenPI 完整数据集工厂还会尝试从 `gs://big_vision/paligemma_tokenizer.model` 下载 tokenizer，当前网络失败/等待，因此未启动训练；不是数据注册或 norm stats 错误。后续需先缓存 tokenizer/base weights，再执行冒烟训练。

## 9. 当前完整训练链路（2026-09-11）

原 RTX4090 24GB 单卡在 Pi0.5 反向训练时 OOM。数据位于持久化 NFS PVC（200GB，约 32GB 已用），不是容器本地盘；新 RTX5090 容器挂载同一 PVC 后复用代码、数据、assets、缓存和权重。新镜像因旧 `.venv` 的 Python 路径失效，重新执行了 `uv sync --no-install-package lerobot` 并安装本地 LeRobot。

基础权重通过本机高速下载后 rsync 到 `/root/shared-nvme/openpi-cache/openpi-assets/checkpoints/pi05_base/params`，目标总量约 12.44GB。RTX5090 的旧 `ptxas` 提示表示首次由驱动编译 PTX，主要影响首次编译速度。

RTX5090 上 100-step smoke 已成功（约 2分39秒），loss/grad_norm 有限且下降，checkpoint：`/root/shared-nvme/checkpoints/pi05_panda_multi_object_lora_smoke/panda_5090_smoke/99`。正式配置当前为 batch=1、action horizon=5、token 上限=64、EMA 关闭、LoRA rank 保持默认；20,000-step 正式训练已启动，step 0→110 的 loss 约 0.098→0.0568。

## 10. 已训练模型接入 MuJoCo（2026-09-11）

新增本机脚本 `scripts/run_mujoco_policy_eval.py`，已上传到新实例 `/root/shared-nvme/workspace/pi05-finetune-lab/scripts/`。脚本直接加载正式 checkpoint，在现有 Panda 场景中逐步渲染外部/腕部相机图像，构造 OpenPI Panda observation，调用 `policy.infer()`，把 8 维动作写入 MuJoCo 控制器，并保存每回合 MP4、JSON 和汇总报告。

同时上传了 `pi05_local`、配置、测试场景和 `mujoco_arm_lab` 依赖；为避免云端访问 GitHub，上传了本机缓存的 Franka Panda menagerie 资产。新容器补装了 EGL/OpenGL 与 `mujoco-menagerie` 运行依赖。

一次 30-step 冒烟验证已实际完成：

```text
checkpoint: .../panda_formal_v1/19999
episodes: 1
successes: 0
success_rate: 0.0
mean_infer_ms: 862.1
video: /root/shared-nvme/logs/mujoco_model_eval_smoke/episode_000.mp4
summary: /root/shared-nvme/logs/mujoco_model_eval_smoke/summary.json
```

这说明“checkpoint → OpenPI policy → MuJoCo 渲染/控制 → 视频/成功率报告”链路已打通；30 步未成功只代表该单场景短时测试未完成抓取，不等同于正式成功率。批量验证命令和结果解释见本机部署指南及本次交接消息。

## 11. 批量模型验证诊断（2026-09-11）

10 个测试回合全部跑满 300 步，结果为 `successes=0`、`success_rate=0.0`。推理平均耗时约 6.3 ms/步（首回合包含 JAX 编译，约 91 ms/步），因此不是推理卡住或视频脚本异常。

对第一个场景做了首步对照：初始 state 的 home 姿态约为
`[0, 0.006, 0, -1.577, 0, 1.570, -0.785, 1]`，模型预测约为
`[-0.005, 0.006, -0.013, -1.564, -0.001, 1.565, -0.802, 1]`，几乎仍停留在 home 姿态。这与视频中“机械臂在上方抖动、不下降”一致，问题在模型输出/训练效果，不是 MuJoCo 控制器没有执行。

训练样本第 0 帧的真实动作同样接近 home，说明字段、状态尺度和动作单位至少在首帧是对齐的；当前最可能原因是 20,000 steps、batch=1 只覆盖 94,064 帧训练集约 0.21 个遍历周期，模型尚未学会完整的长时序接近/下降/抓取动作。下一步应先做小数据过拟合或增加训练步数，并用训练集帧上的预测误差验证，而不是继续修改渲染器。

## 12. 原始权重与过拟合验证（2026-09-11）

为区分基础模型/推理链路与 Panda 微调问题，完成了原始权重、1,000、5,000、10,000 步单回合过拟合对照。原始权重可正常推理但不应作为 Panda 成功基线；1,000 步训练帧 MAE 约 0.0091，5,000 步损失约 0.0059，10,000 步最终损失约 0.003–0.004。10,000 步 checkpoint 为 `/root/shared-nvme/checkpoints/pi05_panda_overfit_1ep/panda_overfit_1ep_v3/9999`。

这些结果确认数据字段、norm stats、Panda 适配、LoRA checkpoint 和推理接口连通。若同场景闭环仍失败，可排除“只是训练步数不够”这一单一解释；剩余核心问题是行为克隆的长时序闭环鲁棒性：训练帧小误差在仿真滚动中累积，当前评估每次只执行预测动作序列第一个动作，且正式训练覆盖不足。继续只训练同一回合没有诊断价值，应检查动作块执行方式并增加多回合、多初始状态数据。


### 12.1 关键定位：提前合爪来自模型输出

对 10,000 步 checkpoint 做了前 50 步动作 trace，并与单回合训练轨迹对照。训练轨迹前约 41 帧夹爪保持张开（action[7]≈1.0），第 42 帧才开始关闭；但模型推理第 15 步已经输出 action[7]≈0.276。MuJoCo 的 `actuator_gripper` 映射为 normalized=1 对应张开，且训练数据与初始状态一致，因此不是仿真夹爪方向或控制器错误。模型在早期视觉/状态输入下没有复现正确的时序动作，随后闭环状态偏离，导致一直抖动并无法抓取。

### 12.2 真实动作回放基线

使用与训练 episode 完全一致的场景和 parquet action，直接驱动 MuJoCo，真实轨迹在第 138 帧成功将苹果放入托盘。由此确认 Panda 场景、关节 action 单位、夹爪方向和物理控制器均可正常工作；失败只发生在模型闭环预测阶段。

### 12.3 闭环恢复数据增强

新增可选参数 `--initial-joint-noise`，在 teacher 采集开始时对 Panda 关节加入均匀初始扰动；原始采集默认仍为 0，不影响旧数据。已上传云端并完成兼容 LeRobot API 的修正。使用 `initial_joint_noise=0.02` 生成了 20 个混合 nominal/recovery episode，20/20 成功，0 次失败，约 4,000 多帧。下一步需将该恢复集转换为兼容视图并与原训练集合并或作为正式模型的恢复微调集。

### 12.4 混合恢复微调结果

构建了 1 个原始 episode + 4 个扰动 episode 的 1126 帧混合诊断集，完成兼容转换和 norm stats，并从正式 Panda checkpoint 做 5000 步恢复微调。最终 checkpoint：`/root/shared-nvme/checkpoints/pi05_panda_mixed_small/panda_mixed_recovery_v1/4999`。

在原始 nominal 场景上的 MuJoCo 结果仍为 0/1。因此初始扰动数据虽能让 teacher 成功生成恢复轨迹，但仅加入少量恢复数据还不足以修复 policy 闭环；在扩大数据前，应继续检查观测图像/状态与训练轨迹的对齐及闭环控制器。

### 12.5 完整 action chunk 插值验证

按第一条方案修改 MuJoCo 评估器：不再只执行模型预测序列的第一个动作，而是执行完整 action chunk，并在每个物理步对关节与夹爪目标做线性插值。使用恢复微调 checkpoint `/root/shared-nvme/checkpoints/pi05_panda_mixed_small/panda_mixed_recovery_v1/4999` 在原始 nominal 场景验证，结果仍为 `success=false`、`success_rate=0.0`（100 个推理步，平均推理约 259 ms，首轮包含编译开销）。

结论：简单的 action chunk 执行/插值不是当前失败的唯一原因，不能靠改评估器解决。Flow Matching 本身已经在模型推理中生成 action chunk，但模型闭环状态漂移后仍输出错误动作。下一步应做观测对齐检查和 policy-induced rollout/DAgger 数据，而不是继续盲目增加同一分布的训练步数。

### 12.6 训练图像与 MuJoCo 观测对齐检查

对训练 episode 0（scene_seed=10000、frame_index=0、timestamp=0.0）用完全相同的 positions/tray 参数重新构建 MuJoCo 场景，并与 parquet 首帧解码图像、状态向量比较。训练图像和渲染图像均为 256×256 RGB；外部相机像素 MAE=0.00072、最大差=5，腕部相机像素 MAE=0.00011、最大差=1；8 维 state 最大绝对误差=3.4e-8。

结论：训练图像、相机视角/颜色通道、状态向量和首帧时间点严格对齐，当前闭环失败不是首帧观测错位。此前模型验证使用 seed=50000，而训练 episode 0 使用 seed=10000，二者是不同场景，不能作为逐像素对照。报告已保存为 `reports/observation_alignment.json` 和 `reports/observation_alignment.md`。

### 12.7 滚动执行 1/3/5 步测试与动作窗口审计（2026-09-12）

在正式 checkpoint `panda_formal_v1/19999` 上，分别测试每次观测后执行预测动作序列的 1、3、5 步，再重新观测。测试场景 seed=50000 的结果分别为 `0/1`、`0/1`、`0/1`。随后使用训练 episode 0 的完全同场景（scene_seed=10000、相同 positions/tray）执行 1 步滚动，仍为 `0/1`。因此改变 action 保持步数不能解决问题，失败也不是单纯 held-out 场景泛化。

进一步审查 OpenPI `data_loader.py`：LeRobot 的 action 序列由 `delta_timestamps={t/fps for t in range(action_horizon)}` 构造，即样本为 `action[t], action[t+1], ...`，没有发现固定的一帧错位。当前云端正式配置的 `model.action_horizon=5`，而 `freeze_filter` 构造使用 10，属于配置不一致，虽然不一定是直接故障，但重新训练时应统一。

当前结论：基础时间保持、首帧观测和 action[t] 顺序均已验证；问题集中在短 horizon/配置不一致与策略闭环学习不足。下一步应先统一 action_horizon（建议 10）并做小规模过拟合/闭环回归，再决定是否正式重训。

### 12.8 action_horizon=20 显存探针（2026-09-12）

新增临时配置 `pi05_panda_h20_mem_probe`，`action_horizon=20`、`max_token_len=64`、batch=1、100 steps，仅用于显存测试。复用了正式数据集已有 norm stats，训练 100/100 步完成，无 OOM；XLA 日志显示编译后工作集约 15.98 GiB，RTX5090 32GB 可运行。探针 checkpoint：`/root/shared-nvme/checkpoints/pi05_panda_h20_mem_probe/h20_mem_probe_v1/99`。这不是正式模型，也不能代表成功率；下一步可在该配置上做小回合过拟合/闭环验证。

### 12.9 action_horizon=50 显存探针（2026-09-12）

新增临时配置 `pi05_panda_h50_mem_probe`，执行 100 步显存测试。`[1,50,32]` 动作序列成功建立，训练 100/100 步完成并保存 checkpoint，无 OOM；XLA 工作集约 15.98 GiB。探针 checkpoint：`/root/shared-nvme/checkpoints/pi05_panda_h50_mem_probe/h50_mem_probe_v1/99`。该结果只说明 50 步在 32GB 显存上可运行，尚未证明闭环抓取效果。

### 12.10 h50 单 episode 过拟合闭环结果（2026-09-12）

使用 1 个完整 Panda episode、`action_horizon=50` 训练 5000 步，loss 从约 0.105 降到 0.003–0.005。使用完全相同的 `overfit_scene.json` 做闭环测试：每次执行 1 步为 `0/1`，每次执行 5 步仍为 `0/1`。视频已下载到本机 `results/h50_overfit/episode_000_exec1.mp4` 和 `episode_000_exec5.mp4`。

结论：h50 显存可行且 teacher-forced loss 很低，但闭环仍失败，说明当前问题不是 horizon=5 太短，也不是单纯训练不够；需要继续定位模型输出/控制执行语义或训练目标问题，不能直接开始正式 h50 训练。

### 12.11 h50 执行 10 步成功（2026-09-12）

针对视频中“exec1 原地抖、exec5 已出现抓取动作”的现象，在同一 h50 过拟合 checkpoint 和同一 overfit 场景上测试每次执行预测序列前 10 步。结果 `success=true`、成功率 `1/1`，第 29 个控制周期完成。成功视频已下载到本机 `results/h50_overfit/episode_000_exec10_success.mp4`。

关键结论：模型并非完全不会抓取；chunk 执行太短会导致频繁重规划，动作推进不足。exec5 已进入抓取阶段，exec10 完成抓取。正式部署应采用“预测较长 chunk、执行约 10 步后再重新观测”的滚动策略，并在多场景正式模型上验证。

### 12.12 修正 chunk 视频时间压缩（2026-09-12）

发现原 receding evaluator 在每个 chunk 只写 1 帧，导致 execute_actions=10 时约 29 秒物理仿真被编码成约 2.9 秒视频。已改为每个 100ms action 写入一帧，并重新生成 `results/h50_overfit/episode_000_exec10_success_fixed.mp4`；模型仍成功 `1/1`。因此原视频过短是录像脚本问题，不是机械臂真实速度过快。

### 12.13 动作抖动平滑验证（2026-09-12）

在 h50 过拟合模型上加入 EMA 动作平滑（`alpha=0.3`，新目标占 30%）以及每个 100ms 控制周期内的关节/夹爪目标线性插值；每次执行 10 步后重新观测。同一场景仍成功 `1/1`，说明平滑没有破坏抓取。视频：`results/h50_overfit/episode_000_exec10_smooth03.mp4`。正式模型应优先采用该执行策略，并在多场景上验证 alpha=0.2/0.3/0.5。

### 12.14 成功后继续录制（2026-09-12）

原评估器检测到物体进入托盘后立即 break，视频因此在放下瞬间结束。新增 `--post-success-cycles`，设置为 30 后继续运行/录制成功后的松手和撤退阶段；h50 平滑模型仍成功 `1/1`。完整视频：`results/h50_overfit/episode_000_exec10_post30.mp4`。

### 12.15 h50 正式模型 50k 阶段评测（2026-09-12）

正式 checkpoint `panda_h50_formal_v1/50000` 在同一批 40 个冻结普通测试场景上的结果为：`alpha=0.2` 34/40（85%）、`alpha=0.3` 31/40（77.5%）、`alpha=0.5` 30/40（75%）、`alpha=1.0` 32/40（80%）。普通场景中 `alpha=0.2` 最好，但只比无平滑多 2 个成功回合。

同一批 40 个 recovery 场景中，`alpha=0.2` 为 22/40（55%），`alpha=1.0` 为 30/40（75%）。关闭 EMA 后，`approach_offset`、`object_slip`、`weak_grasp` 分别提升 2、3、3 个成功回合，`place_offset` 仍为 5/10。说明强平滑的响应滞后明显妨碍恢复动作；后续应测试弱平滑或自适应平滑，并从 50k 继续到 75k 做同场景配对评测。

完整结果、EMA 原理、结论和后续判据见 `reports/h50_formal_50k_stage_report.md`。
