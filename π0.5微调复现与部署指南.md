# π0.5 自采数据微调与仿真部署指南

> 更新日期：2026-09-10  
> 主路线：本地自定义 Panda/MuJoCo 采集数据，算力平台完成 π0.5 JAX LoRA，再由远端模型闭环控制本地仿真。

## 0. 最终决定

### 项目任务

> **在带干扰物的随机桌面场景中，控制 Panda 抓起指定物体并放入绿色盒子。**

模型使用的英文指令：

~~~text
Pick up the {target} and place it in the green box.
~~~

选择这个任务的原因：

- 单次抓取和放置，便于先把完整流程做正确；
- 苹果、托盘和干扰物的位置可以随机化，能形成自己的数据集；
- 苹果使用现有真实网格，场景不是纯色方块演示；
- 可加入抓偏、滑落、放偏等可恢复错误，研究闭环纠错；
- 成功条件明确，适合自动统计成功率。

### 仿真与数据从哪里来

当前方案直接使用本地 mujoco_arm_lab：

~~~text
MuJoCo：计算 Panda、物体、托盘、碰撞和接触
现有视觉与规划控制器：充当数据采集教师
我们新增的数据记录器：保存图像、状态、动作和指令
π0.5：使用这些数据学习正常操作和纠错
~~~

不再使用 LIBERO 官方示范作为训练数据。LIBERO 可以保留为参考，但不是本项目主线。

### 一眼看懂完整流程

~~~text
第一阶段：全部在本地完成

确定任务和接口
  → 加绿色托盘和腕部相机
  → 优化教师的搬运路径和时间参数
  → 把教师改成可重规划状态机
  → 采集正常成功示范
  → 注入可恢复扰动并采集纠错示范
  → 写成 LeRobot 数据集并回放检查
  → 本地验收通过

第二阶段：申请算力平台

上传数据
  → 安装 OpenPI
  → 配置 Panda 数据适配和 JAX LoRA
  → 100 step 冒烟训练
  → 正式训练并下载 checkpoint

第三阶段：本地与算力平台配合

算力平台运行 π0.5 policy server
  ↔ SSH 隧道
本地 MuJoCo client 执行动作并评测正常与扰动场景
~~~

本地阶段的具体工作单独写在：

> [算力平台申请前的本地工作清单](./算力平台申请前的本地工作清单.md)

在该清单的“申请算力平台门槛”通过前，不租 GPU。

### 本文会用到的几个词

| 词 | 在本项目中的含义 |
| --- | --- |
| LeRobot 数据集 | 一种统一的机器人数据目录和字段格式，让 OpenPI 知道每帧图像、机器人状态、教师动作和指令如何对应。它不是把 MuJoCo 或机器人“转换”掉。 |
| `pi05_base` | 已预训练的 π0.5 基础模型起点；它还不知道我们自定义的 Panda 数据字段和动作含义。 |
| JAX LoRA | 用 OpenPI 的 JAX 训练程序，只训可学习的小型低秩适配参数，而不是重新训整个模型；目的是降低显存和训练成本。 |
| norm stats | 用自有数据计算的数值分布统计，用于缩放 state/action，避免不同关节的数值范围差异干扰训练。 |
| checkpoint（检查点） | 某一训练步保存的模型参数和相关配置。它是可用于评测或继续训练的版本，不是数据集检查报告。 |
| policy server | 算力平台上加载 checkpoint 的推理服务；本地 MuJoCo 发观测给它，它返回后续动作。 |
| action chunk | 模型一次预测的一小段连续动作。为了闭环纠错，本地只执行前几步就重新观测，而不是一次全部执行完。 |

## 1. 本地电脑与算力平台分工

| 位置 | 工作 |
| --- | --- |
| **本地电脑** | 修改 MuJoCo 场景、优化可重规划教师、分片并行采集、注入扰动、合并并写成 LeRobot、回放检查、运行部署 client、保存评测视频 |
| **算力平台** | 安装 OpenPI、计算 norm stats、运行 JAX LoRA、保存 checkpoint、运行 policy server |
| **两端配合** | 上传数据、建立 SSH 隧道、远程请求动作、下载 checkpoint 与日志 |

本地 RTX 3050 不负责 π0.5 训练。它负责 MuJoCo 渲染、数据采集和模型部署后的客户端。

## 2. 为什么以规划教师为主，而不是手工采集

规划控制器通常比手工操作更适合生成第一批数据：

- 动作平滑、时间对齐稳定；
- 可以用不同随机种子自动运行数百次；
- 容易保证绝大多数示范最终成功；
- 可以准确记录每个时刻真正发给 actuator 的动作。

但只收规划器的完美轨迹也有明显问题：模型只见过正常状态，一旦抓偏、滑落或动作受到扰动，可能不知道怎样返回正确轨迹。

因此本项目采用混合数据，而不是二选一：

| 数据类型 | 建议比例 | 作用 |
| --- | ---: | --- |
| 正常规划成功示范 | 75% | 学习基本接近、抓取、搬运和放置 |
| 扰动后恢复并最终成功的示范 | 25% | 学习偏离正常轨迹后的纠错 |
| 不可恢复失败 | 不进入第一版训练集 | 用于分析和压力测试 |

第一版固定采用 **75% 正常成功 + 25% 纠错成功**；四类纠错各占总数据约 6.25%。失败回合只进入诊断集，不能混入监督训练集。

手工操作不作为主要数据来源。人工主要负责观察画面、确认教师动作合理、给失败分类，并在自动教师无法覆盖的特殊恢复状态上少量补充。

### 2.1 教师轨迹也必须先优化

现有 `panda_vision_grasp.py` 将搬运拆成固定的“竖直抬到 0.54 m → 同高度水平移动 → 竖直下降”。它适合验证抓放链路，但不应直接成为正式训练示范。

第一版教师的目标是“安全约束下的高质量近似最优路径”，不宣称数学上的全局最优：

- 抓起后仍保留必要安全抬升，但高度根据通道中障碍物最高点和安全余量自适应，不再固定为 0.54 m；
- 从抓取后位姿到放置前位姿生成三次 Bézier 或 B-spline 三维弧线，让 XY 搬运与 Z 高度变化同时发生；
- 沿弧线采样 TCP 位姿，用前一帧关节角作为下一帧 IK 初值，并拒绝关节越界、自碰和障碍物碰撞路径；
- 对通过安全检查的候选路径，按执行时间、TCP/关节路程、加速度与 jerk、最小净空打分，选择总代价最低的一条；
- 最后再做关节速度和加速度限制下的时间参数化，避免几何路径平滑但执行时抽动。

正式数据不能只按“最终成功”筛选，还要对路径长度、耗时、最大加速度/jerk、最小碰撞净空和不必要停顿设门槛。恢复动作可以更保守，但也必须经过同样的平滑性和安全检查。

## 3. 纠错数据怎样生成

行为克隆只能学习数据中出现过的“当前状态 → 正确动作”。只给正常动作加随机噪声，而不记录后续正确恢复过程，不会自动产生纠错能力。

正确做法是：

~~~text
教师正常执行
    ↓
在指定阶段注入一个可恢复扰动
    ↓
教师重新观察当前画面和状态
    ↓
重新定位目标并规划
    ↓
最终完成任务
    ↓
保存从扰动前到恢复成功的完整 episode
~~~

第一版加入四类扰动：

1. **接近偏移**：在预抓取阶段给末端目标加入小的 XY 偏移，教师随后重新对准；
2. **抓取未夹稳**：第一次闭合不足，教师检测没有抬起苹果后，张开夹爪、撤离并重抓；
3. **搬运中轻微滑落**：在抬升或移动阶段给苹果一个小扰动，教师重新观察并重抓；
4. **放置偏移**：第一次放到托盘边缘，教师检测苹果不在有效区域后重新抓取并放置。

教师必须从“一次性预计算完整轨迹”升级为状态机：

~~~text
观察
 → 接近
 → 抓取
 → 验证是否抓住
 → 抬升
 → 验证是否滑落
 → 放置
 → 验证是否在托盘内
 → 成功

任一验证失败
 → 安全撤离
 → 重新观察
 → 重新规划
 → 最多重试 2 次
~~~

碰撞严重、苹果飞出桌面、关节越界或重试两次仍失败的回合只保存到失败诊断集，不进入第一版监督训练集。

## 4. 模型输入和输出

第一版固定为 10 Hz：

| 字段 | 内容 |
| --- | --- |
| image | 一台固定外部相机的 256×256 RGB |
| wrist_image | 一台安装在 Panda hand 下的 256×256 RGB |
| state | 7 个关节位置 + 1 个归一化夹爪位置，共 8 维 |
| action | 下一控制时刻的 7 个绝对关节目标 + 1 个归一化夹爪目标，共 8 维 |
| task | 固定英文指令 |

数据集保存 256×256 图像；PandaInputs 再按固定 OpenPI commit 的模型要求做 resize/pad。训练和推理必须调用同一套图像预处理。

其他外部相机、深度图、物体真值位置、接触量和教师内部阶段可以保存为诊断信息，但不作为 π0.5 第一版输入。

观测必须在动作下发前读取。每条训练数据表达：

~~~text
看到 observation_t 时，应执行 action_t
~~~

OpenPI 内部动作宽度可以大于 8。Panda 数据适配层负责把真实 8 维数据填入模型接口，并在输出时只取真实 8 维。第一版从数据记录到仿真执行都保持“绝对关节目标”，不做 delta 变换；如果后续要比较 delta action，必须同时增加训练前变换和推理后逆变换，不能只改一端。

## 5. 自有数据集设计

建议第一版正式数据：

| 划分 | 正常成功 | 恢复成功 | 用途 |
| --- | ---: | ---: | --- |
| train | 300 | 100 | 训练 |
| validation | 40 | 20 | 选 checkpoint、检查过拟合 |
| test | 不保存教师动作 | 50 个正常场景 + 50 个扰动场景 | 最终闭环评测 |

这不是硬性理论下限。正式采集前先生成：

- 5 个 episode：检查文件是否能写；
- 30 个 episode：检查回放、时序和小规模过拟合；
- 教师连续 50 个随机场景达到至少 90% 成功率后，再批量采集。

场景至少随机化：

- 苹果初始 XY 和小角度旋转；
- 3 个随机干扰物的位置和组合；
- 托盘小范围位置变化；
- 光照强度和方向；
- 外部相机与腕部相机的小幅位姿扰动；
- 接近方向和抓取高度的小范围变化。

训练、验证和测试按 scene seed 分开。测试 seed 不生成训练动作，也不能在调试过程中反复用来选参数。

## 6. LeRobot 在这里做什么

LeRobot 是训练数据的标准组织方式。我们采集的数据最终整理为：

~~~text
episode 0
  frame 0: image, wrist_image, state, action, task
  frame 1: image, wrist_image, state, action, task
  ...
episode 1
  ...
~~~

还应在 episode 元数据中保存：

~~~text
scene_seed
success
recovery_type
perturbation_step
retry_count
teacher_version
camera_config_version
~~~

这些恢复标签用于分组评测和诊断，第一版不直接输入模型。

## 7. 申请算力平台的门槛

以下条件全部满足后再租 GPU：

- 任务、相机、state 和 action 语义已经冻结；
- 外部相机和腕部相机都能稳定看到任务关键区域；
- 10 Hz 图像、state 和 action 严格对齐；
- 教师可以验证抓取、滑落和放置结果并重新规划；
- 至少 30 个自采 episode 能完整回放；
- 正常和恢复 episode 都已出现；
- LeRobot 数据集能被本地 loader 读取；
- train/validation/test seed 无交叉；
- 数据无 NaN、Inf、明显动作跳变或 RGB/BGR 错误；
- 数据集清单、大小、校验和和版本说明已经生成。

详细命令和产物见本地工作清单。

## 8. [算力平台] GPU 选择

OpenPI 官方给出的单卡显存量级：

| 工作 | 显存要求 | 选择 |
| --- | ---: | --- |
| 推理 | > 8 GB | 4090 24GB 足够 |
| JAX LoRA | > 22.5 GB | 4090 可从 batch 1 测试；48GB 更稳 |
| 全量微调 | > 70 GB | 本项目不做 |

推荐 Ubuntu 22.04、单张 L40S/A6000/RTX 6000 Ada 48GB、64GB CPU 内存和至少 200GB 数据盘。预算紧时先租 4090 24GB 做 100 step 测试。

## 9. [两端配合] 上传本地成果

上传：

~~~text
LeRobot 数据集
数据 manifest 和校验和
Panda 输入输出适配代码
训练配置
相机与动作接口说明
本地教师版本号
~~~

使用 rsync：

~~~bash
rsync -av /local/pi05_upload/ user@server:/workspace/pi05_upload/
~~~

上传后在两端分别计算校验和，确认没有缺失或损坏文件。

## 10. [算力平台] 安装 OpenPI

~~~bash
mkdir -p /workspace/pi05-lab
cd /workspace/pi05-lab
git clone --recurse-submodules https://github.com/Physical-Intelligence/openpi.git
cd openpi
git rev-parse HEAD | tee ../OPENPI_COMMIT.txt

GIT_LFS_SKIP_SMUDGE=1 uv sync
GIT_LFS_SKIP_SMUDGE=1 uv pip install -e .
export OPENPI_DATA_HOME=/workspace/openpi-cache
export HF_LEROBOT_HOME=/workspace/lerobot
~~~

OpenPI commit 必须与本地准备适配代码时使用的版本一致。

## 11. [算力平台] Panda 数据适配与 LoRA

需要在固定 OpenPI commit 上加入：

- PandaInputs：把双路 RGB、8 维 state、8 维 action 和 prompt 映射为模型字段；
- PandaOutputs：只返回实际使用的前 8 维动作；
- LeRobotPandaDataConfig：定义字段和 norm stats；第一版明确使用 absolute joint target，不做 delta 变换；
- pi05_panda_multi_object_lora_smoke：与正式版接口相同，但 `num_train_steps=100`；
- pi05_panda_multi_object_lora：正式 JAX LoRA 训练配置，步数在冒烟和小数据过拟合通过后再定。

使用 pi05_base 作为起点。因为本项目的相机、动作语义和数据都是自定义的，不直接假设 pi05_libero 或 pi05_droid 能零样本控制当前环境。

先计算本数据集自己的 norm stats：

~~~bash
uv run scripts/compute_norm_stats.py --config-name pi05_panda_multi_object_lora
~~~

再做 100 step 冒烟：

~~~bash
XLA_PYTHON_CLIENT_MEM_FRACTION=0.9 uv run scripts/train.py pi05_panda_multi_object_lora_smoke --exp-name=multi_object_box_smoke --overwrite
~~~

`pi05_panda_multi_object_lora_smoke` 必须在配置中明确设置 `num_train_steps=100`。100 step 只验证数据、模型、显存和 checkpoint 保存流程。通过后再确定正式 batch、步数和租用时间。

## 12. [算力平台] 启动模型服务

~~~bash
uv run scripts/serve_policy.py policy:checkpoint --policy.config=pi05_panda_multi_object_lora --policy.dir=/workspace/checkpoints/pi05_panda_multi_object_lora/multi_object_box_v2/STEP --port=8000
~~~

服务端只负责接收 observation 并返回 action chunk。

## 13. [两端配合] 闭环部署

本地建立 SSH 隧道：

~~~bash
ssh -N -L 8000:127.0.0.1:8000 user@server-address
~~~

本地 MuJoCo client 每次发送双路图像、state 和 prompt。模型返回动作块后，本地先执行前 3～5 步，再重新观察并请求新动作。

本地必须保留安全层：

- 关节限位；
- 单步 action jump 限制；
- 超时保持当前姿态；
- 碰撞或物体飞出工作区时终止；
- 可重复 reset；
- 完整录像与状态日志。

## 14. 评测

分别评测：

| 测试集 | 回合数 | 目的 |
| --- | ---: | --- |
| 未见过的正常随机场景 | 50 | 基本任务成功率 |
| 接近偏移 | 15 | 是否会重新对准 |
| 未夹稳 | 15 | 是否会重新抓取 |
| 搬运滑落 | 10 | 是否能寻找并重抓 |
| 放置偏移 | 10 | 是否会修正放置 |

同时记录阶段性指标：看见目标、到达预抓取点、双指有效接触、成功抬升、搬运未掉落、进入托盘、最终稳定和总成功。

对比至少包括：

1. 只用正常示范训练的 checkpoint；
2. 正常 + 恢复示范训练的 checkpoint；
3. 规则教师。

这个对比能直接说明恢复数据是否带来纠错能力，是本项目最有价值的实验之一。

## 15. 最终交付物

- 自己采集的 LeRobot 数据集；
- 正常与恢复数据比例及采集说明；
- 可重规划教师与扰动注入器；
- Panda/OpenPI 数据适配；
- LoRA 配置、norm stats 和 checkpoints；
- 本地远程推理 client；
- 正常与扰动测试成功率；
- 成功、失败和恢复案例视频；
- 固定的代码 commit、数据版本和复现实验命令。

## 16. 官方参考

- [OpenPI 仓库与显存要求](https://github.com/Physical-Intelligence/openpi)
- [OpenPI 自定义数据微调主流程](https://github.com/Physical-Intelligence/openpi#fine-tuning-base-models-on-your-own-data)
- [OpenPI DROID 双视图和 Panda 状态接口示例](https://github.com/Physical-Intelligence/openpi/tree/main/examples/droid)
- [OpenPI UR5 自定义适配示例](https://github.com/Physical-Intelligence/openpi/tree/main/examples/ur5)
- [OpenPI 远程推理文档](https://github.com/Physical-Intelligence/openpi/blob/main/docs/remote_inference.md)
- [OpenPI LeRobot 转换示例](https://github.com/Physical-Intelligence/openpi/blob/main/examples/libero/convert_libero_data_to_lerobot.py)
